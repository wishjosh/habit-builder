{
  "name": "habit-builder",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "습관 형성 시스템 — 작은 실천을 함께 쌓는 가족용 앱",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check engine.js && node --check storage.js && node --check cloud.js && node --check sw.js && node --check hierarchy.js && node --check planner-ui.js"
  }
}
